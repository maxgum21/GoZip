# BST in Go

I tried to realise a binary search tree.
However i came to a small problem when trying to delete a tree node.
When writing functions with recievers i can't easily delete the node that is being recieved.

So i took a roundabout way, and wrote it just like i would write it in C.
